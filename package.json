{
  "name": "email-api-serverless",
  "version": "1.0.0",
  "description": "Serverless email API",
  "main": "handler.js",
  "scripts": {
    "start": "serverless offline start",
    "deploy": "serverless deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "serverless-offline": "^12.0.4"
  }
}